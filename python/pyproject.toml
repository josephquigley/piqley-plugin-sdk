[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "piqley-plugin-sdk"
version = "0.1.0"
description = "SDK for building piqley plugins"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
